#include <array>
#include <odom_check/msg/odom.hpp>

// static std::unique_ptr<odom_check::msg::odom> get_frame(const float r, const float p, const float y, const float x, const float y)
// {
//   auto frame = std::make_unique<odom_check::msg::odom>();
//   frame->roll = r;
//   frame->pitch = p;
//   frame->yaw = y;
//   frame->x = x;
//   frame->y = y;

//   return frame;
// }